title: Claude Code Mastery
description: A resource for Claude Code — configuration, architecture, and training
theme: jekyll-theme-midnight

# Build settings
markdown: kramdown
highlighter: rouge

# Exclude files from build
exclude:
  - README.md
  - LICENSE
  - .gitignore
  - "*.docx"

# Default layout for all pages
defaults:
  - scope:
      path: ""
    values:
      layout: default
